package com.example.mymessenger.network.models;

public enum MessageType {
    PASSWORD_REQUEST,
    PASSWORD,
    PASSWORD_ACCEPTED,
    PASSWORD_NOT_ACCEPTED,
    NAME_REQUEST,
    USER_NAME,
    NAME_ACCEPTED,
    NAME_NOT_ACCEPTED,
    TEXT,
    USER_ADDED,
    USER_REMOVED
}
